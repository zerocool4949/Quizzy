services:
  quizzy:
    build: .
    ports:
      - "7111:7111"
    volumes:
      - ./server/movies.json:/app/server/movies.json:ro
      - audio-cache:/app/server/audio-cache
    environment:
      - NODE_ENV=production
      - PORT=7111

volumes:
  audio-cache:
